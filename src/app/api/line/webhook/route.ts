// LINE OA webhook handler
// Handles: message.text, postback, follow, unfollow events

export async function POST(request: Request) {
  // TODO: Implement LINE webhook handler
  return Response.json({ ok: true });
}
